import React from "react";
import styled from "styled-components";
import { CardRectangle, CardSquare } from '../components'

const Banner = () => {
  return (
    <div>
      <CardSquare></CardSquare>
    </div>
  );
};

export default Banner;
